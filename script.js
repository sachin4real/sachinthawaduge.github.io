document.getElementById("contact-form").addEventListener("submit", function (event) {
    event.preventDefault();
    // Add your code to handle the form submission (e.g., sending data to a server)
    alert("Form submitted!");
});

